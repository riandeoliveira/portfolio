export interface Colors {
  primary: string;
  primary_light: string;
  secondary: string;
  neutral_dark: string;
  neutral: string;
}

export interface DomainLevelBarProps {
  progress: number;
}

export interface Fonts {
  primary: string;
  secondary: string;
  tertiary: string;
  quaternary: string;
  default: string;
}

export interface ParallaxBoxProps {
  image: string;
}
