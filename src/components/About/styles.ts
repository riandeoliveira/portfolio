import styled from "styled-components";
import { sla } from "../../assets";

export const AboutBox = styled.section`
  p {
    color: white;
    font-family: "Roboto";
    line-height: 1.5em;
    margin: auto;
    max-width: 800px;
  }
`;
